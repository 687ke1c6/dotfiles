#!/data/data/com.termux/files/usr/bin/bash

# Acquire the Termux CPU wake lock
termux-wake-lock

# Optional: Start your background services here
# Example: dufs /sdcard/ -A --port 5000 >> /sdcard/dufs.log 2>&1 &